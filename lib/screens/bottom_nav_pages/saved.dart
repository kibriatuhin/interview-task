import 'package:flutter/material.dart';
class SavedScreens extends StatelessWidget {
  const SavedScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bro I am SaveScreens"),);
  }
}
