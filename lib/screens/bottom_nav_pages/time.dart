import 'package:flutter/material.dart';
class TimerScreens extends StatelessWidget {
  const TimerScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bro I am Timer"),);
  }
}
