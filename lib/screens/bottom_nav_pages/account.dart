import 'package:flutter/material.dart';
class AccountScreens extends StatelessWidget {
  const AccountScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bro I am Account"),);
  }
}
